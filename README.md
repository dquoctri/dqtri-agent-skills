# dqtri-agent-skills
Zero-install Hermes-lite is a lightweight agent skill system for vibe coding that installs shared coding rules, reusable skills, and per-project memory across multiple AI coding tools without duplicating prompt context.
